# Java 後端快速啟動

## ✅ 好消息

資料庫密碼已經設定好了！現在只需要：

## 🚀 步驟 1：下載依賴

### 方法 A：使用 IntelliJ IDEA（推薦 - 最簡單）

1. **下載 IntelliJ IDEA Community（免費）**
   - https://www.jetbrains.com/idea/download/
   
2. **開啟專案**
   - File > Open > 選擇 `java-backend` 資料夾
   - IntelliJ 會自動下載依賴（等待右下角進度條完成）

3. **啟動應用程式**
   - 右鍵點擊 `src/main/java/com/heath/api/Application.java`
   - 選擇 "Run 'Application.main()'"
   - 或點擊右上角綠色三角形 ▶️

### 方法 B：使用 VS Code + Maven

如果已安裝 Maven：

```powershell
cd java-backend
mvn dependency:copy-dependencies -DoutputDirectory=lib
mvn spring-boot:run
```

## ✅ 步驟 2：確認後端已啟動

應該會看到：
```
=========================================
葡眾愛客戶管理系統 - 後端 API 已啟動
API 端點: http://localhost:8080/api
=========================================
```

## ✅ 步驟 3：測試 API

在瀏覽器訪問：
```
http://localhost:8080/api/customers?userId=test-user-id
```

應該會返回 JSON（可能是空陣列 `[]`）。

## ✅ 步驟 4：前端已設定完成

我已經幫您：
- ✅ 更新 `config.js` 為 `java-api`
- ✅ 重新加入 `java-api-client.js` 載入

## 🎯 現在可以測試

1. **啟動後端**（使用 IntelliJ 或 Maven）
2. **開啟前端**（`html/login.html`）
3. **登入系統**
4. **測試功能**

## 🔍 如果遇到問題

### 問題：找不到 Maven
**解決：** 使用 IntelliJ IDEA（會自動處理）

### 問題：後端無法啟動
**檢查：**
- Java 版本：`java -version`（需要 11+）
- 資料庫密碼已設定（已完成）

### 問題：前端無法連接
**檢查：**
- 後端是否在運行（`localhost:8080`）
- 瀏覽器控制台是否有錯誤

## 📝 推薦流程

1. **下載 IntelliJ IDEA Community**（約 5 分鐘）
2. **開啟 `java-backend` 資料夾**
3. **點擊 Run** ▶️
4. **完成！**

需要我協助哪個步驟？

