# 葡眾愛客戶管理系統

## 📋 專案簡介

這是一個使用 HTML、CSS、JavaScript 建立的客戶管理系統，資料儲存在 Supabase 資料庫中。

## 🚀 快速開始

### 啟動方式

1. **直接開啟 HTML 檔案**
   - 前往 `html` 資料夾
   - 雙擊 `login.html` 開啟登入頁面

2. **使用本地伺服器（推薦）**
   ```bash
   cd html
   python -m http.server 8000
   ```
   然後訪問：http://localhost:8000/login.html

3. **使用 VS Code Live Server**
   - 安裝 "Live Server" 擴充功能
   - 右鍵點擊 `html/login.html` > "Open with Live Server"

### 第一次使用

1. 註冊新帳號（`register.html`）
2. 登入系統
3. 開始使用：新增客戶、管理行程、記錄訂單

## 📁 專案結構

```
heath/
├── html/                    # 前端檔案（主要目錄）
│   ├── login.html          # 登入頁面 ⭐
│   ├── register.html       # 註冊頁面
│   ├── calendar.html       # 行事曆主頁
│   ├── customers.html      # 客戶資料列表
│   ├── add-customer.html   # 新增客戶
│   ├── orders.html         # 訂單管理
│   ├── css/               # 樣式檔案
│   └── js/                # JavaScript 檔案
│       ├── config.js      # 設定檔（重要）
│       ├── supabase-client.js  # Supabase 客戶端
│       └── ...
├── java-backend/           # Java 後端（可選，目前未使用）
└── supabase/              # SQL 腳本和說明
```

## ⚙️ 設定

### Supabase 設定

確認 `html/js/config.js` 中的設定：

```javascript
const SUPABASE_URL = 'https://lvrcnmvnqbueghjyvxji.supabase.co';
const SUPABASE_ANON_KEY = '您的 Supabase Key';
const DATA_SOURCE = 'supabase'; // 使用 Supabase
```

### 資料庫結構

系統使用以下資料表：
- `users` - 使用者資料（使用 `user_login` 欄位串聯）
- `customers` - 客戶資料
- `schedules` - 行程資料
- `orders` - 訂單資料

所有資料表都使用 `user_login` 欄位進行資料隔離。

## 🔧 功能說明

- ✅ 使用者註冊和登入
- ✅ 客戶資料管理（新增、編輯、刪除）
- ✅ 行程管理
- ✅ 訂單管理
- ✅ 資料隔離（每個使用者只能看到自己的資料）

## 📝 技術說明

### 資料串聯

系統使用 `user_login` 欄位進行資料串聯：
- `users.user_login` = `customers.user_login` = `schedules.user_login` = `orders.user_login`

詳細說明請參考：`user_login_串聯說明.md`

### Java 後端（可選）

如果未來需要使用 Java 後端，請參考 `java-backend/README.md`。

## 🐛 疑難排解

### 無法登入
- 檢查 Supabase 設定是否正確
- 檢查瀏覽器控制台是否有錯誤

### 資料無法顯示
- 確認已登入
- 檢查資料表中是否有 `user_login` 欄位

### 前端無法連接
- 確認 Supabase 專案正常運行
- 檢查網路連線

## 📄 授權

本專案為內部使用。

