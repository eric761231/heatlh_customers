# Netlify 部署配置
# 注意：HTML 版本已移除，如需部署請使用 Flutter Web
# 
# 部署 Flutter Web 版本：
# 1. 執行：cd flutter_app && flutter build web
# 2. 設定 publish = "flutter_app/build/web"
# 3. 設定 command = "cd flutter_app && flutter build web"

[build]
  # 如需部署 Flutter Web，取消下面的註解並設定正確路徑
  # publish = "flutter_app/build/web"
  # command = "cd flutter_app && flutter build web"

