[build]
  # 發布目錄設為 html 資料夾
  publish = "html"
  # 建置命令（可選，因為是靜態檔案）
  command = ""

# 重定向規則
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200

# SPA 路由支援（如果未來需要）
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200

